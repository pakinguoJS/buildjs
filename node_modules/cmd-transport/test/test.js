var transport = require('../lib/cmd-transport.js');

var fs = require('fs');
var alias = fs.readFileSync('../conf/transport-alias.json', 'utf8');
try{
    alias = JSON.parse(alias);
}catch(e){
    alias = null;
}

transport('D:/proj', 'D:/proj_dist', alias);